import 'package:flutter/material.dart';

class TableScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
        child: Scaffold(
      appBar: AppBar(
        title: Text("TableScreen"),
      ),
      body: const Center(
        child: Text("No implementation for TableScreen yet!"),
      ),
    ));
  }
}
